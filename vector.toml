[sources.docker_logs]
type = "docker_logs"

[sinks.axiom]
type = "http"
inputs = ["docker_logs"]
uri = "https://api.eu.axiom.co/v1/datasets/lexibot/ingest"
encoding.codec = "json"
headers.Authorization = "${AXIOM_BEARER_TOKEN}"
headers.Content-Type = "application/json"
